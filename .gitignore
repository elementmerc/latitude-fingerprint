# The proprietary Broadcom driver is fetched on install, never committed
# (see docs/LICENSING.md).
*.deb

# Downloaded driver artefacts. install.sh --keep-download writes here when run
# from a clone; the tarball is not covered by the *.tar.gz rule below.
.cache/
*.tgz

# Source-package build artefacts
shape-b/build/
*.dsc
*.changes
*.buildinfo
*.tar.gz
*.tar.xz

# Secrets
.env
.env.*
*.pem
*.key
.credentials.json

# Editor and OS clutter
.DS_Store
Thumbs.db
*.swp
*.swo
.idea/
.vscode/
